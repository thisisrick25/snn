# GPU (CUDA 13.2) build of torch/torchvision from the PyTorch index. The
# --extra-index-url keeps the other packages resolving from PyPI. For a CPU-only
# install, drop the +cu132 local versions and the extra index (use torch==2.13.0
# torchvision==0.28.0 from PyPI, or the older 2.12.0/0.27.0 CPU pins).
--extra-index-url https://download.pytorch.org/whl/cu132
torch==2.13.0+cu132
torchvision==0.28.0+cu132
snntorch==0.9.4
numpy==2.4.6
scikit-learn==1.9.0
matplotlib==3.11.0
pandas==3.0.3
scipy==1.17.1
PyYAML==6.0.3
